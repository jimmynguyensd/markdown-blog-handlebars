<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Static Blog</title>
    <style>
        body { font-family: sans-serif; line-height: 1.6; margin: 2em; max-width: 800px; margin-left: auto; margin-right: auto; }
        ul { list-style: none; padding: 0; }
        li { margin-bottom: 1em; }
        h2 { margin-bottom: 0.5em; }
        p { margin-top: 0.5em; color: #555; }
    </style>
</head>
<body>
    <header>
        <nav>
            <a href="/">Home</a>
            {{#each pages}}
                <a href="/{{this.slug}}/">{{this.metadata.title}}</a>
            {{/each}}
            <a href="/authors/">Authors</a>
        </nav>
        <hr>
        <h1>Welcome to My Static Blog!</h1>
    </header>
    <main>
        <h2>Latest Posts</h2>
        <ul>
            {{#each posts}}
            <li>
                <h3><a href="/{{category}}/{{slug}}.html">{{metadata.title}}</a></h3>
                <p>Published: {{metadata.publishedDate}} by {{metadata.author}}</p>
                <p>{{metadata.description}}</p>
            </li>
            {{/each}}
        </ul>
    </main>
    <footer>
        <p>&copy; 2025 My Static Blog</p>
    </footer>
</body>
</html>