<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{metadata.title}}</title>
    <meta name="description" content="{{metadata.description}}">
    <style>
        body { font-family: sans-serif; line-height: 1.6; margin: 2em; max-width: 800px; margin-left: auto; margin-right: auto; }
        img { max-width: 100%; height: auto; }
    </style>
</head>
<body>
    <header>
        <nav>
            <a href="/">Home</a>
            {{#each pages}}
                <a href="/{{this.slug}}/">{{this.metadata.title}}</a>
            {{/each}}
        </nav>
        <hr>
        <h1>{{metadata.title}}</h1>
        <p>By {{metadata.author}} on {{metadata.publishedDate}}</p>
    </header>
    <article>
        {{{content}}} {{!-- Triple braces for unescaped HTML --}}
    </article>
    <footer>
        <p><a href="/">Back to Home</a></p>
    </footer>
</body>
</html>