<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About {{ profile.name }}</title>
    <style>
        body { font-family: sans-serif; line-height: 1.6; margin: 2em; max-width: 800px; margin-left: auto; margin-right: auto; }
        img { max-width: 100%; height: auto; }
        .author-profile { background-color: #f9f9f9; padding: 1.5em; border-radius: 8px; margin-bottom: 2em; }
        .author-profile h1 { margin-top: 0; }
        .social-links a { margin-right: 1em; }
    </style>
</head>
<body>
    <header>
        <nav>
            <a href="/">Home</a>
            {{#each pages}}
                <a href="/{{this.slug}}/">{{this.metadata.title}}</a>
            {{/each}}
            <a href="/authors/">Authors</a>
        </nav>
    </header>
    <main>
        <div class="author-profile">
            <h1>{{ profile.name }}</h1>
            {{#if profile.title}}
                <h2>{{ profile.title }}</h2>
            {{/if}}

            {{{ content }}} 

            {{#if profile.social}}
            <div class="social-links">
                <strong>Connect:</strong>
                {{#each profile.social}}
                    <a href="{{this}}" target="_blank">{{@key}}</a>
                {{/each}}
            </div>
            {{/if}}
        </div>

        <hr>

        <h2>Posts by {{ profile.name }}</h2>
        {{#each posts}}
        <article>
            <h3><a href="/{{this.category}}/{{this.slug}}/">{{this.metadata.title}}</a></h3>
            <p>{{this.metadata.description}}</p>
            <p><em>Published on {{this.metadata.publishedDate}}</em></p>
        </article>
        {{/each}}

        {{#unless posts}}
            <p>This author has not published any posts yet.</p>
        {{/unless}}

    </main>
    <footer>
        <p>&copy; 2025 footer here</p>
    </footer>
</body>
</html>