<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts by {{authorName}}</title>
    <style>
        body { font-family: sans-serif; line-height: 1.6; margin: 2em; max-width: 800px; margin-left: auto; margin-right: auto; }
        img { max-width: 100%; height: auto; }
    </style>
</head>
<body>
    <header>
        <h1>Posts by {{authorName}}</h1>
        <nav>
            <a href="/">Home</a>
            {{#each pages}}
                <a href="/{{this.slug}}/">{{this.metadata.title}}</a>
            {{/each}}
            <a href="/authors/">Authors</a>
        </nav>
    </header>
    <main>
        {{#each posts}}
        <article>
            <h3><a href="/{{this.category}}/{{this.slug}}/">{{this.metadata.title}}</a></h3>
            <p>{{this.metadata.description}}</p>
            <p><em>Published on {{this.metadata.publishedDate}}</em></p>
        </article>
        <hr>
        {{/each}}
    </main>
    <footer>
        <p>&copy; 2025 footer here</p>
    </footer>
</body>
</html>